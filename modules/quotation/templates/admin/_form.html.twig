{{ form_start(form) }}
    {{ form_widget(form) }}

<span id="js-data" data-source="{{ path('quotation_admin_autocompletion_ajax') }}"></span>
<span id="js-search" data-source="{{ path('quotation_admin_search', {'query' : ' '}) }}"></span>


<div class="d-flex justify-content-center">
    <button class="btn btn-primary">
        {{ button_label|default('Enregistrer') }}
        <!-- Boutton pour enregistrer le devis -->
    </button>
</div>

{{ form_end(form) }}

