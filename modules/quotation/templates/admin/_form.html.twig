{{ form_start(form) }}
{{ form_widget(form) }}

<span id="js-data" data-source="{{ path('quotation_admin_ajax', {'id_customer' : 0 }) }}"></span>

<div class="d-flex justify-content-center">
    <button class="btn btn-primary">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>

{{ form_end(form) }}