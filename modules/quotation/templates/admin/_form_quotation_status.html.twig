<div class="quotation-status-section">
    {{ form_start(formQuotationStatus, {'attr':{'class':'mt-2'}}) }}

    <div class="d-flex align-items-center">
        {{ form_label(formQuotationStatus.status, null, {'label_attr':{'class':'col-sm-6 mt-2'}}) }}
        <div class="col-sm-12">
            {{ form_widget(formQuotationStatus.status, {'attr':{'class':'quotation-status-select mt-2'}}) }}
        </div>
    </div>

    <div class="d-flex justify-content-center">
        <button id="submitCreateNewQuotation" type="button" class="btn btn-light d-flex mt-4">
            <span uk-icon="icon: check" class="mr-1"></span> Créer un devis
        </button>
    </div>

    {{ form_end(formQuotationStatus) }}
</div>