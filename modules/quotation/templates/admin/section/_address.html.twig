<div id="js-output-address" class="d-none">

    <div class="quotation card p-4">
        <div class="quotation-head mb-3 d-flex">
            <div class="d-flex align-items-center">
                <i class="material-icons">location_on</i>
                <h1 class="panel-heading">Adresses</h1>
            </div>
        </div>

        <div class="d-flex mt-4">
            <div class="d-flex flex-column col-lg-6">

                <label for="address-delivery">
                    <h2><i class="material-icons mr-2">local_shipping</i> Livraison</h2>
                </label>

                <select name="address-delivery" id="address-delivery" class="delivery">
                    <!-- alias addresses -->
                </select>

                <div class="card address-card mt-3 mb-4" id="address-delivery-selected">
                    <!--- address delivery -->
                </div>
            </div>

            <div class="d-flex flex-column col-lg-6">

                <label for="address-invoice">
                    <h2><i class="material-icons mr-2">description</i> Facture</h2>
                </label>

                <select name="address-invoice" id="address-invoice" class="invoice">
                    <!-- alias addresses -->
                </select>

                <div class="card address-card mt-3 mb-4" id="address-invoice-selected">
                    <!-- address invoice -->
                </div>
            </div>
        </div>

    </div>

</div>